<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top shadow">
    <div id="main-navbar" class="container">
        <!-- NAVBAR TOGGLER -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fas fa-bars text-light"></i>
        </button>

        <!-- NAVBAR BRAND -->
        <a class="navbar-brand text-center ms-5 ms-lg-0" href="/">
            <img src="/images/logo.png" class="img-fluid" alt="" class="src" style="max-width: 96px;">
        </a>

        <!-- CART SM -->
        <button class="navbar-toggler position-relative me-3 me-md-0">
            <i class="fas fa-shopping-cart text-light"></i>
            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                3
                <span class="visually-hidden">items in your cart.</span></span>
        </button>

        <!-- NAVBAR ITEMS -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mb-2 mb-lg-0 text-center">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/faqs">FAQs</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="#" data-bs-toggle="modal" data-bs-target="#contactModal">Contact</a>
                </li>
                <% if (currentUser && currentUser.userLevel === 2) { %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        Manage
                    </a>
                    <ul class="dropdown-menu dropdown-menu-dark w-25 mx-auto text-center"
                        aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="/admin/faqs">FAQs</a></li>
                        <li><a class="dropdown-item" href="#">Orders</a></li>
                        <li><a class="dropdown-item" href="/admin/products">Products</a></li>
                        <li><a class="dropdown-item" href="/admin/users">Users</a></li>
                    </ul>
                </li>
                <% } %>
            </ul>
            <ul class="navbar-nav ms-auto text-center">
                <% if (!currentUser) { %>
                <li class="nav-item">
                    <a href="#" data-bs-toggle="modal" data-bs-target="#registerModal" class="nav-link"><i
                            class="fas fa-user-plus"></i>&#160;&#160;Register</a>
                </li>
                <li class="nav-item">
                    <a href="#" data-bs-toggle="modal" data-bs-target="#loginModal" class="nav-link">
                        <i class="fas fa-sign-in-alt"></i>&#160;&#160;Login
                    </a>
                </li>
                <% } else { %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        <%if (currentUser.userLevel === 2) { %>
                        <i class="fas fa-crown"></i>&#160;&#160;
                        <% } else { %>
                        <i class="fas fa-user-circle"></i>&#160;&#160;
                        <% } %>
                        <%= currentUser.firstName %>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-dark w-25 mx-auto text-center"
                        aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="/admin/faqs">Profile</a></li>
                        <li><a class="dropdown-item" href="/admin/faqs">Orders</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a href="/logout" class="nav-link">
                        <i class="fas fa-sign-out-alt"></i>&#160;&#160;Logout
                    </a>
                </li>
                <% } %>
                <!-- CART LG -->
                <li class="nav-item">
                    <button id="lg-cart" class="btn btn-outline-light position-relative d-none d-lg-block me-2"
                        style="top: 3px;" data-bs-toggle="modal" data-bs-target="#cartModal">
                        <i class="fas fa-shopping-cart text-light"></i>
                        <span class="position-absolute start-100 translate-middle badge rounded-pill bg-danger"
                            style="top: 5px;"><%= cartQty %></span>
                        <span class="visually-hidden">items in your cart.</span>
                    </button>
                </li>
            </ul>
        </div>
    </div>
</nav>